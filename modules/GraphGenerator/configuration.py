from modules.GraphGenerator.Consts import BFS_EXECUTOR, THREADING, TIME_BETWEEN_THREADS

configuration = {
    BFS_EXECUTOR: {
        THREADING: {
            TIME_BETWEEN_THREADS: 0.5
        }
    }
}
